# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=1.10.0,<2.0.0
python-dotenv>=1.0.0
PyJWT>=2.8.0
httpx>=0.27.0
# --- Security / auth (shared models import these) ---
cryptography>=42.0.0
passlib[bcrypt]>=1.7.4
# --- Database (shared models) ---
SQLAlchemy>=2.0.0
asyncpg>=0.29.0
# --- AI / LLM ---
anthropic>=0.25.0
openai>=1.30.0
# --- NLP pipeline (Legal-BERT + spaCy) ---
# torch CPU-only to keep image size manageable (~2 GB saved vs CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
transformers>=4.38.0
spacy>=3.7.0
numpy>=1.26.0
# --- Caching ---
redis>=5.0.0
